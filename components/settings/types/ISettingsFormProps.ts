export interface ISettingsFormProps {
  // No external props yet, the form reads state via the storage adapter.
  // Kept as an empty marker interface so future props slot in without touching consumers.
  className?: string;
}
