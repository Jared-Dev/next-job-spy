# Optional fallback Anthropic API key.
# Local mode authenticates AI calls through the Claude Code CLI (your Pro/Max
# subscription) — no key needed. This is only used if the subscription
# rate-limits. The in-app /settings entry overrides this value.
ANTHROPIC_API_KEY=
