/**
 * Default fallback for the @modal parallel slot. When no intercepting route
 * is active (e.g. on initial load of the job page) the slot renders nothing.
 */
export default function Default() {
  return null;
}
