export enum ESourceId {
  Greenhouse = 'greenhouse',
  Lever = 'lever',
  RemoteOk = 'remoteok',
  WeWorkRemotely = 'wwr',
  /** A job entered by hand (e.g. pasted from LinkedIn), no fetch adapter. */
  Manual = 'manual',
}
